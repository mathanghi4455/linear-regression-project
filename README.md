# linear-regression-project
This project implements a basic Linear Regression model using Python to demonstrate how machine learning can be used for prediction. The model is trained on a small dataset consisting of input values (X) and corresponding outputs (y). After training, the model predicts results for new input values provided by the user.
